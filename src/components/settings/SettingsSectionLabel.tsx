/**
 * Shared settings sheet section label (Safety Center / Support).
 */

export function SettingsSectionLabel({ t }: { t: string }) {
  return (
    <div className="mt-3.5 mb-1 px-1 text-[10px] uppercase tracking-[0.12em] text-[#8B9099] leading-none">
      {t}
    </div>
  );
}
