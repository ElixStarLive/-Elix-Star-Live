---
description: LOCK — Comments panel is final; never edit without explicit owner order
globs: src/components/EnhancedCommentsModal.tsx
alwaysApply: true
---

# Comments panel — LOCKED (owner 2026-08-08)

The video **Comments** sheet UI is **done and approved**. Do **not** change it.

## Locked files (do not edit)

- `src/components/EnhancedCommentsModal.tsx`
- Do not change comment-only sizing, layout, header, sort placement, split bar, handle, badge, name, icons, composer, or body text
- Do not reintroduce comment marquee / walking text
- Do not `git restore` / `git checkout` this file unless the owner explicitly asks to restore

## Current approved header (do not rearrange)

- Top drag handle pill (same as More Options: `w-10 h-1 rounded-full bg-white/25`)
- **Newest** on the left
- **Comments** title centered (no icon)
- **Oldest** + **Most Liked** on the right
- One split line under the header only (not between comment rows)

## Forbidden unless the owner explicitly asks in that message

- No layout, spacing, sizing, color, typography, or icon changes on comments
- No “small polish”, alignment tweaks, or matching other screens
- No drive-by edits while fixing something else

## Allowed only with an explicit new order

If the owner names **comments** / **comment panel** and a specific change in the same message, do **only** that change.

Default: **never touch comments**.
