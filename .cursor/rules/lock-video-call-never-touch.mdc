---
description: LOCK — Video call + incoming call UI is final; never edit without explicit owner order
globs: src/pages/VideoCall.tsx,src/components/IncomingCallModal.tsx
alwaysApply: true
---

# Video call — LOCKED (owner 2026-08-08)

The **video call** screen and **incoming call** modal are **done and approved**. Do **not** change them.

## Locked files (do not edit)

- `src/pages/VideoCall.tsx`
- `src/components/IncomingCallModal.tsx`
- Do not `git restore` / `git checkout` these files unless the owner explicitly asks to restore

## Current approved behavior (do not rearrange)

### VideoCall
- Top-right close ends the call and returns to that person’s DM thread (`ensureDmThread` → `/inbox/:threadId`)
- Red **End call** button (`#EF4444` + PhoneOff) ends call and returns to messages
- Green **Accept** (`#22C55E` + Phone) only when `status === 'incoming'`
- Mic / camera / switch camera controls stay as approved
- Avatar + name + status (“Calling…”) when remote video is not connected
- Local PiP top-right

### IncomingCallModal
- Red decline + green accept
- Accept starts call and opens `/call`

## Forbidden unless the owner explicitly asks in that message

- No layout, spacing, sizing, color, typography, or icon changes
- No “small polish” or matching other screens
- No drive-by edits while fixing something else
- No patches, leftovers, or workarounds on these files

## Allowed only with an explicit new order

If the owner names **video call** / **VideoCall** / **incoming call** and a specific change in the same message, do **only** that change.

Default: **never touch video call**.
