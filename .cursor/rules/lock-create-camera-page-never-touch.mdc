---
description: LOCK — Create camera page is final; never edit without explicit owner order
globs: src/components/ElixCameraLayout.tsx,src/pages/Create.tsx,src/components/CaptureShutterButton.tsx
alwaysApply: true
---

# Create camera page — LOCKED (owner 2026-08-05)

The Create camera UI is **done and approved**. Do **not** change it.

## Locked files (do not edit)

- `src/components/ElixCameraLayout.tsx`
- `src/pages/Create.tsx`
- `src/components/CaptureShutterButton.tsx`
- Related Create-camera-only CSS for `.camera-rail-disc`, `.camera-right-rail`, `.capture-shutter-ring`, `.elix-silver-red-text` in `src/index.css` — do not change those rules unless the owner explicitly names them

## Forbidden unless the owner explicitly asks in that message

- No layout, spacing, sizing, color, typography, or icon changes on this page
- No “small polish”, alignment tweaks, or matching other screens
- No drive-by edits while fixing something else
- No undoing / redoing ring, disc, text gradient, close, Add sound, Upload, POST/CREATE/LIVE, or shutter styling

## Allowed only with an explicit new order

If the owner names this page and a specific change in the same message, do **only** that change.

Default: **never touch this page**.
