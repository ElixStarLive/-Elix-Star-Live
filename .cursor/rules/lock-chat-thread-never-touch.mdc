---
description: LOCK — Chat thread page is final; never edit without explicit owner order
globs: src/pages/ChatThread.tsx
alwaysApply: true
---

# Chat thread — LOCKED (owner 2026-08-08)

The DM **Chat thread** page UI is **done and approved**. Do **not** change it.

## Locked file (do not edit)

- `src/pages/ChatThread.tsx`
- Do not `git restore` / `git checkout` this file unless the owner explicitly asks to restore

## Current approved layout (do not rearrange)

### Header
- Top handle pill (same as More Options: `w-10 h-1 rounded-full bg-white/25`)
- Video call left, **avatar ring centered on top** (size **48**), back right
- **Username under** the ring (centered)
- **No level** in the header
- Silver split line under header (`border-[#D8D9DD]/45`)

### Messages (every sender)
- Writing starts from the **left**
- Each message shows **ring → level → name**, then message body
- Message LevelBadge: `circleSize={30}`, `size={16}`
- Do not move message identity back into the header
- Do not reintroduce right-aligned “me” bubbles unless the owner asks

### Composer
- Keep existing “Type a message…” bar and send control as approved

## Forbidden unless the owner explicitly asks in that message

- No layout, spacing, sizing, color, typography, or icon changes on chat thread
- No “small polish”, alignment tweaks, or matching other screens
- No drive-by edits while fixing something else
- No patches, leftovers, or workarounds on this page

## Allowed only with an explicit new order

If the owner names **chat thread** / **ChatThread** / **DM chat** and a specific change in the same message, do **only** that change.

Default: **never touch chat thread**.
