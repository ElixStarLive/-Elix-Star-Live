---
description: LOCK — Inbox page is final after cleanup; never edit without explicit owner order
globs: src/pages/Inbox.tsx
alwaysApply: true
---

# Inbox — LOCKED (owner 2026-08-08)

The **Inbox** page is **done and approved** after patch/leftover cleanup. Do **not** change it.

## Locked file (do not edit)

- `src/pages/Inbox.tsx`
- Do not `git restore` / `git checkout` this file unless the owner explicitly asks to restore

## Cleanup contracts already applied (do not reintroduce)

- No `elix_inbox_deleted_*` localStorage tombstones — server delete only
- No `elix_avatar_*` localStorage avatar fallback
- No fabricated follower stubs (`username: 'user'`)
- No legacy like/comment notification fallback for Activity — `/api/activity` only
- Follow notifications are not duplicated as Archive system rows (New followers hub owns that)
- One shared `deleteConversation` for Main + Unread
- Named exit: close → `/feed` (never `history.back()`)
- No dead filter sliders control

## Forbidden unless the owner explicitly asks in that message

- No layout, spacing, sizing, color, typography, or icon changes
- No “small polish” or matching other screens
- No drive-by edits while fixing something else
- No patches, leftovers, or workarounds

## Allowed only with an explicit new order

If the owner names **Inbox** and a specific change in the same message, do **only** that change.

Default: **never touch Inbox**.
