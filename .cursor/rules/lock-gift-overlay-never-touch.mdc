---
description: LOCK — Gift overlay animation + size final; never edit without explicit owner order
globs: src/components/GiftOverlay.tsx,src/components/GiftAnimationOverlay.tsx,src/components/LiveGiftFeedStack.tsx
alwaysApply: true
---

# Gift overlay — LOCKED (owner 2026-08-09)

Gift **video overlay**, **red banner**, **feed stack**, and **gift size/framing** are **done and approved**. Do **not** change them.

## Locked files (do not edit)

- `src/components/GiftOverlay.tsx`
- `src/components/GiftAnimationOverlay.tsx`
- `src/components/LiveGiftFeedStack.tsx`
- Do not `git restore` / `git checkout` these files unless the owner explicitly asks to restore

## Locked framing contract (memorize — never “improve”)

Approved solo gift video framing in `GiftOverlay`:

- Container: `fixed left-0 right-0 mx-auto w-full max-w-[480px]`
- Solo height: `calc(70% - 25mm)` from `bottom: 0`
- Solo video fit: **`object-cover`** (full-width fill of that frame — never `object-contain`)
- Solo mask: fade at top (`linear-gradient` mask as committed)
- Battle side-scope half-pane logic may stay; do not change solo size/fit

Red banner (`GiftAnimationOverlay`): full column width, edge-to-edge (`left-0 right-0`, no side padding on the bar wrapper).

## Creator + spectator must stay identical

Both must use the **same** components with the **same** size-affecting props:

- `LiveHostScreen.tsx` → `GiftOverlay` + `GiftAnimationOverlay` + `LiveGiftFeedStack`
- `SpectatorLiveScreen.tsx` → same three, same framing (no per-page width/height/object-fit/z-index size hacks)

Do **not** fork a second gift video overlay for spectator or creator.

## Forbidden unless the owner explicitly asks in that message

- No gift animation, size, scale, object-fit, max-width, height, mask, or positioning changes
- No “make gifts bigger/smaller/fuller” drive-bys
- No theme remaps that restyle gift overlays
- No patches, leftovers, or workarounds on these files
- No touching gift overlay while fixing buy-coins, Join, battle bar, top bar, or anything else

## Allowed only with an explicit new order

If the owner names **gift overlay** / **GiftOverlay** / **gift animation** / **gift size** and a specific change in the **same message**, do **only** that change.

Default: **never touch gift overlay animation or gift size.**
